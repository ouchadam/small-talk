name: Assemble

on:
  push:
    branches:
      - 'main'
  pull_request:

jobs:
  assemble-debug:
    uses: ouchadam/small-talk/.github/workflows/with_env.yml@ref
    name: Assemble debug variant
    runs-on: ubuntu-latest

    steps:
      - name: Assemble debug variant
        run: ./gradlew assembleDebug --no-daemon
